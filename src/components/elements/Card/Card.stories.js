import React from "react";
import { storiesOf } from "@storybook/react";
import Card from "./Card";

storiesOf("Components|Elements/Card", module).add("default", () => (
  <Card>Card</Card>
));
